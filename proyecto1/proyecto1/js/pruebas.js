/**
 * 
 *
 * @param {number} a
 * @param {number} b
 * @return {number} 
 */
function suma(a, b) {
  debugger;
  return a + b;
}

/**
 * @type {number} x
 * @type {number} y
 * */
let x = 5,
  y = 6;
console.log(suma(x, y));
